# Requirements for femap-api-python
# NOTE: `pyfemap` is usually provided by the Femap application/SDK and may not be available on PyPI.
numpy>=1.24,<2
matplotlib>=3.7
pandas>=2
scipy>=1.10
pywin32>=305

